# Requirements
```
pip install opencv-python
pip install -r requirements.txt
```

# Work flow
git add --all
git commit -m "<message>"
git pull
<fix merge conflict
<git add --all
<git commit -m "<message>"
<git pull

git push
